package pl.shockah.dunlin.plugin;

import pl.shockah.plugin.PluginManager;

public class Plugin extends pl.shockah.plugin.Plugin<Plugin> {
	public Plugin(PluginManager<Plugin> manager, Info info) {
		super(manager, info);
	}
}