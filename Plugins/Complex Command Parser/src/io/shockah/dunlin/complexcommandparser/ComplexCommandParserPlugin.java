package io.shockah.dunlin.complexcommandparser;

import io.shockah.dunlin.plugin.Plugin;
import io.shockah.dunlin.plugin.PluginManager;

public class ComplexCommandParserPlugin extends Plugin {
	public ComplexCommandParserPlugin(PluginManager manager, Info info) {
		super(manager, info);
	}
}